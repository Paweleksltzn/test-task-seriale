import { HeightEqualizerDirective } from './height-equalizer.directive';

describe('HeightEqualizerDirective', () => {
  it('should create an instance', () => {
    const directive = new HeightEqualizerDirective();
    expect(directive).toBeTruthy();
  });
});
